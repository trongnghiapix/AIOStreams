services:
  aiostreams:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aiostreams
    restart: unless-stopped
    ports:
      - "10875:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
